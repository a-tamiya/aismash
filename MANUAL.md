# プロンプトファイターズ 実行マニュアル

AIが生成したキャラクターで戦う2D対戦格闘ゲーム「プロンプトファイターズ（AI Prompt Fighter）」の、実行環境・起動方法についてのマニュアルです。

## 1. 想定しているハードウェア環境

- **GPU**: 特別な要件はありません。DirectX 11/12に対応した一般的なGPU（内蔵GPUでも可）で動作します。
- **コントローラー**: **Xbox系ゲームパッドが必須**です（最低1つ。2人対戦をする場合は2つ）。
  - 現在のUI・チュートリアルはゲームパッド操作を前提にした表示になっています（キーボード操作の案内はUI上から削除済みです）。
  - スマッシュ攻撃は右スティックのチルト操作のみに対応しており、キーボードでは代替できません。
  - 移動などごく一部の操作はキーボード（1P: A/D、2P: 矢印キー）でも反応しますが、上記の理由から実質的にゲームパッド前提の設計です。
- **カメラ（Webカメラ）**: 不要です。
- **マイク**: 対戦中に出現する「ボイスボール」機能（マイクに話しかけた願いをAIが解釈してゲームに反映する演出）で使用します。ロビー設定画面で機能自体をON/OFFできるため、マイクが無い環境でもOFFにすれば問題なく遊べます。
- **スピーカー**: 必要です（BGM・効果音・AI実況・ボイスボール応答音声などを使用します）。
- **モニター**: 16:9ワイドスクリーン、1920×1080以上を推奨します（開発・動作確認は2560×1440で実施）。ウィンドウは全画面表示で起動します。
- **ネットワーク（インターネット接続）**: キャラクターの新規AI生成・ボイスボール機能・AI実況を使う場合は必須です（OpenAI APIを利用します）。**インターネット接続が無くても、既に保存済みのキャラクターでの対戦・トレーニング・チュートリアルは問題なく遊べます。**

## 2. 実行環境について（特定PCの構築環境に依存しません）

本アプリは**Unity Editorのインストールを必要としないスタンドアロンビルド**として配布しています。特定の研究室PCで構築済みの開発環境（Unity Editor等）を使う前提ではありません。そのため、PCのログインパスワードを一時的に共有していただく必要もありません。

**Windows 10/11（64bit）が動作する任意のPCであれば、下記の手順ですぐに動かせます。**

## 3. 環境構築法（他PCで実行する際の準備）

1. GitHubリポジトリのReleaseページから配布用zipファイルをダウンロードし、任意のフォルダに展開してください。
   （リポジトリ内に`Build`フォルダがある場合は、そのフォルダを丸ごとコピーしても同様に動作します。`.exe`単体ではなく、フォルダ一式が必要です。）
2. （任意）AIによるキャラクター新規生成機能を使う場合のみ、実行するPCに環境変数 `OPENAI_API_KEY` を設定してください。
   - Windowsの検索で「環境変数を編集」を開き、ユーザー環境変数に `OPENAI_API_KEY` を追加
   - 設定後はアプリを（既に起動していた場合は一度終了して）起動し直してください
   - 未設定でもエラーメッセージが出るだけで、その他の機能（対戦・トレーニング・チュートリアル・既存キャラでのプレイ）は問題なく動作します
3. ゲームパッド（Xbox系推奨）をPCに接続してください。

## 4. 起動方法

展開したフォルダ内の `aismash_unity.exe` をダブルクリックするだけで起動します。コマンドライン操作は不要です。

## 5. 起動後の基本操作

アプリ内にゲームパッド操作を使ったチュートリアルが用意されているため、基本的な操作方法（移動・ジャンプ・ガード・技・スマッシュ・つかみ・ボイスボール）はそちらを参照してください。ロビー画面から「1vs1対戦」「ボス討伐（協力）」「トレーニング」「チュートリアル」「AIキャラ生成」を選択できます。

## 6. その他注意点

- **APIキーの取り扱い**: 複数PCに同じ`OPENAI_API_KEY`を配布する場合、利用料金・利用上限は共通になります。OpenAIのダッシュボードで使用量上限（Usage limits）を設定しておくことを推奨します。またキーを含む設定ファイル等を誤って公開・共有しないよう注意してください。
- **通信速度への依存**: AIキャラ生成・ボイスボールの解釈・AI実況は通信環境に依存します。回線が遅い場合、ボイスボールの反応に15〜25秒程度かかることがあります。
- **AI生成失敗時の挙動**: 通信エラー等でキャラクター生成に失敗した場合は、入力した名前でローカルの簡易生成（技は簡略化されたもの）に自動的に切り替わり、そのまま保存されます。
- **保存済みキャラクターについて**: チュートリアル用「ケンゴ」「サヤ」・固定ボス「冥王ゾルバイン」の3体は初回起動時に自動で使用可能になります。それ以外に各PCで生成したキャラクターは、そのPC内にのみ保存されます（他PCへは自動で同期されません）。
- **著作権について**: プレイヤーが自由入力でキャラクターを生成できる仕様上、実在の著作物に似たキャラクターが生成・保存される場合があります。公開の場（オープンキャンパス等）で使用する際は、表示するキャラクターの内容にご留意ください。
